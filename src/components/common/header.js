import * as React from 'react';

const Header = () => (
  <h1 style={{
    backgroundColor: '#666',
    color: 'white',
    textAlign: 'center',
    padding: '12px',
  }}
  >
Watch order of Star Wars episodes
  </h1>
);

export default Header;
